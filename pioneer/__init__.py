"""Pioneer/Fastino fine-tune integration point for Kindred.

This package is a FALLBACK stub. The real owner (P2) is responsible for
replacing `finetune.pioneer_predict` with an actual Pioneer/Fastino API call.
Everything else in here (the F1 comparison harness) is a real, working
standalone demo that runs today with the stdlib only.
"""
