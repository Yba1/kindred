# Kindred backend — Workstream A
# Core service. fastapi/uvicorn/pydantic/numpy are required; google-genai is
# installed by default but imported behind a guard (config.gemini_sdk_available),
# so the app still boots and serves the full demo if it is absent or if
# GEMINI_API_KEY is unset. Actian's SDK stays optional the same way.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
numpy>=1.26
python-dotenv>=1.0

# Gemini: profiling + match reasons + embeddings. Runtime-optional — every call
# site falls back to the deterministic path when it is missing or unconfigured.
# This is the current Google GenAI SDK (`from google import genai`); the legacy
# `google-generativeai` package is deprecated and is NOT used.
google-genai>=1.0

# --- test-only ---
# pytest
# httpx            # required by fastapi.testclient

# --- optional sponsors (imports are guarded; safe to omit) ---
# actian-vector    # Actian vector store (falls back to numpy cosine)
